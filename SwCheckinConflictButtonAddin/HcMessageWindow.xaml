<Window x:Class="SwCheckinConflictButtonAddin.HcMessageWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SwCheckinConflictButtonAddin"
        Width="440"
        MinWidth="360"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FontFamily="Microsoft YaHei UI"
        FontSize="13"
        Background="White"
        PreviewKeyDown="OnPreviewKeyDown">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <local:HandyControlTheme/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Margin="20,20,20,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="KindMark"
                    Width="36" Height="36"
                    CornerRadius="18"
                    VerticalAlignment="Top"
                    Margin="0,0,14,0">
                <TextBlock x:Name="KindGlyph"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="White"
                           Text="!"/>
            </Border>
            <TextBlock x:Name="MessageText"
                       Grid.Column="1"
                       TextWrapping="Wrap"
                       Foreground="#606266"
                       LineHeight="22"
                       MaxWidth="360"
                       VerticalAlignment="Center"/>
        </Grid>
        <Border Grid.Row="1" BorderBrush="#EBEEF5" BorderThickness="0,1,0,0" Padding="16,12" Background="White">
            <Button x:Name="OkButton"
                    Content="确定"
                    Width="88" Height="32"
                    HorizontalAlignment="Right"
                    Style="{DynamicResource ButtonPrimary}"
                    IsDefault="True"
                    Click="OnOkClick"/>
        </Border>
    </Grid>
</Window>
